# Backend Environment Variables

# POVINNÉ: tajemství pro šifrování uložených uživatelských OpenAI klíčů (AES-256-GCM)
# Vygeneruj např.: openssl rand -base64 32
APP_SECRET="replace-with-a-long-random-string"

# VOLITELNÉ (doporučené): povol registraci jen konkrétním účtům (ty + žena)
# Uživatelská jména jsou lowercased; odděluj čárkou.

# VOLITELNÉ: max počet účtů (default 2)
# Povolení uživatelé (bez registrace): pouze přihlášení mara/baru
# Hodnoty mohou být buď plaintext heslo, nebo bcrypt hash (začíná na $2...)
AUTH_MARA_PASSWORD="replace-this-password-or-bcrypt-hash"
AUTH_BARU_PASSWORD="replace-this-password-or-bcrypt-hash"

# VOLITELNÉ: server fallback OpenAI klíč (použije se jen pro demo/fallback režim, pokud je v kódu aktivní)
OPENAI_API_KEY="sk-..."

# Doporučené pro Vercel / více instancí: perzistentní sessions + rate limiting
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

PORT=3001
NODE_ENV=development